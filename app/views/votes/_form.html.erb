<%= form_with(model: vote) do |form| %>
  <% if vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vote.errors.count, "error") %> prohibited this vote from being saved:</h2>

      <ul>
        <% vote.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :votable_id %>
    <%= form.number_field :votable_id %>
  </div>

  <div class="field">
    <%= form.label :votable_type %>
    <%= form.text_field :votable_type %>
  </div>

  <div class="field">
    <%= form.label :voter_id %>
    <%= form.number_field :voter_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
